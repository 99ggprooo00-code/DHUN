# GitHub Support Ticket — Workflow re-registration request

**Submit to:** https://support.github.com/contact (select "Actions" →
"Workflow bug / registration issue") or via GitHub Community in the
Actions category. Copy the **Subject** and **Body** below verbatim.

---

## Subject

Workflow 348098190 is wedged: name stuck at file path, Actions UI
entry missing, schedule silent since 09-07, phantom 0-job push runs
on every push — force re-registration or delete+recreate.

## Body

**Repository:** `99ggprooo00-code/DHUN` (private)
**Workflow file:** `.github/workflows/rot-drill.yml`
**Workflow registry id:** `348098190`
**Workflow path (API):** `https://api.github.com/repos/99ggprooo00-code/DHUN/actions/workflows/348098190`
**Expected workflow `name:`:** `rot-drill`
**Expected triggers:** `schedule: '17 4 * * *'` (daily 04:17 UTC) + `workflow_dispatch:` (no `push:` trigger, ever)
**Last healthy scheduled run:** `34083253658` on 2026-09-07 04:28 UTC (conclusion: failure — pre-existing extractor issue, that part is ours)
**Last successful file edit:** `da9d779` on 2026-09-07 06:17 UTC — decay started around this edit.

### Symptoms (all verified via API 2026-09-17)

1. **Actions UI entry missing.** There is no `rot-drill` item in the
   Actions sidebar and no "Run workflow" button. The Actions URL
   `https://github.com/99ggprooo00-code/DHUN/actions/workflows/rot-drill.yml`
   returns a 404 for a logged-in repo owner. This has been
   user-confirmed twice (2026-09-16 and 2026-09-17).

2. **REST registry shows the name as the FILE PATH, not the
   `name:` value.** `GET /repos/99ggprooo00-code/DHUN/actions/workflows/348098190`
   returns:
   ```json
   {
     "id": 348098190,
     "name": ".github/workflows/rot-drill.yml",
     "path": ".github/workflows/rot-drill.yml",
     "state": "active",
     "created_at": "2026-09-02T04:29:15Z",
     "updated_at": "2026-09-17T02:21:08Z"
   }
   ```
   The file's `name: rot-drill` is correct; the registry is not
   picking it up.

3. **Daily schedule has been silent since 2026-09-07 04:28 UTC.**
   Cron `17 4 * * *` fired daily 09-02 → 09-07 (6 runs, the last
   being 34083253658). **≥10 further 04:17 UTC windows (09-08 →
   09-17) never fired,** despite daily pushes to `main`. The
   workflow remains `state: active` the entire time.

4. **Phantom 0-job push runs on EVERY push since 2026-09-07 05:56
   UTC.** Each push creates an `event: push` run that finishes in
   0 seconds with `conclusion: failure`, `failure_reason: null`,
   and **zero jobs** (`jobs.total_count: 0`). The file has **never
   declared a `push:` trigger** in any version (verified at SHAs
   from 2026-09-01 through 56324f5). Recent examples:
   - 35135427771 on PR #74 merge push (2026-09-16)
   - 35163767716 on PR #75 merge push (2026-09-16)
   - 35170942908 on PR #76 merge push (2026-09-17)
   - 35171317970 on PR #77 merge push (2026-09-17)
   - 35174080320 on PR #79 merge push (2026-09-17)

5. **File-content changes do not update the entry.** Two fixes were
   attempted, both failed:
   - **Attempt 1 (comment-only edit, PR #77 = `3ff3a55`, merged
     2026-09-17 01:03 UTC):** Added a 9-line comment block; the
     workflow file is otherwise unchanged. The registry entry was
     not touched — the `updated_at` timestamp remained frozen at
     2026-09-16T23:57:41Z (a different push), name stayed as the
     file path, and phantom push run 35171317970 fired.
   - **Attempt 2 (delete + verbatim re-add, PR #78 = `df6a0be`
     then PR #79 = `56324f5`, merged 2026-09-17 ~02:20 UTC):**
     Removed the file entirely (entry briefly dropped from
     `gh workflow list` for ~90 seconds) then re-added the file
     VERBATIM plus one updated comment block (verified via
     `diff`; triggers and jobs are completely unchanged). When
     the file reappeared on `main`, GitHub reattached the
     **same** wedged registry id `348098190` (not a new id),
     `updated_at` advanced to 2026-09-17T02:21:08Z but `name`
     remained as the file path (not `rot-drill`), the Actions UI
     entry did not return, and phantom push run 35174080320
     fired on the merge. So even a delete+re-add cycle could not
     evict the wedged registration — it appears to be keyed by
     file path.

6. **Integration tokens cannot affect this.** Agent sessions using
   the standard GitHub Actions `GITHUB_TOKEN` get `HTTP 403` on
   `workflow_dispatch` and on `POST /disable` / `/enable`. The
   repo owner's only available action in the Actions UI is Merge
   on PRs — there is literally no Run-workflow button to click
   because of symptom (1). We have no owner-scoped PAT available.

### Requested action

Please **force a clean re-registration** of this workflow entry:

- Option A (preferred): Reset / re-sync registry entry 348098190
  so that it re-reads the file's `name: rot-drill`, registers the
  `schedule` and `workflow_dispatch` triggers (and does NOT
  register the phantom `push` trigger), and restores the Actions
  UI entry.
- Option B: Delete the entry entirely on your side so that the
  next push re-creates it as a fresh workflow with a new id.

After the fix we will verify by (1) checking `gh workflow list`
shows a workflow named `rot-drill` (not the file path), (2)
confirming the Actions UI shows the entry with a Run-workflow
button, (3) watching the next 04:17 UTC window fire (or manually
dispatching), and (4) confirming no further 0-job push-phantom
runs appear.

Thank you!

---

## After-submission checklist

Once support confirms the reset, record:

- [ ] Support ticket URL / case number: __________
- [ ] Date of fix: __________
- [ ] New workflow id (if regenerated): __________
- [ ] Manual `workflow_dispatch` run id & verdict: __________
- [ ] First scheduled 04:17 UTC run id & verdict: __________

Then update `.ai/ROADMAP.md` CURRENT ACTIVE TASK,
`.ai/KNOWN_LIMITATIONS.md`, `docs/runbooks/rot-drill.md`, and
append the verdict line to `docs/verification/14-release.md`.
