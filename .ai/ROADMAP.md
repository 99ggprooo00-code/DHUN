# CURRENT ACTIVE TASK

Updated **2026-09-10 (UTC)** · Arena session **`arena/01a08676-dhun`**.

**Phase / active scope:** Phase 14 extraction reliability follow-up: reconcile the stale handoff, document the Android/Windows stream-resolution asymmetry, and research an Android anonymous-playback root fix. The immediate research target is **PO-token / BotGuard-backed InnerTube extraction**, using **vivi-music (`vivizzz007/vivi-music`) / InnerTubeX** as the concrete reference. Login/cookies remain an optional alternative for authenticated/personalized playback, not an assumed prerequisite for anonymous playback.

**Exact files/tasks currently being worked on:** `.ai/ROADMAP.md`; `.ai/EXTRACTION_AUTH_RESEARCH.md`; next implementation candidates are the Android extraction layer plus safe resolve/cache reliability work. **No extraction identity-chain change is being implemented yet** because ADR-001..006 and the project rule requiring an ADR + real data for extraction identity/parallelism changes still apply.

**Last actual error:** no new code/CI error in this session. Historical last code error remains the first PR #50 Android unit-test failure (`:app-android:testDebugUnitTest`, run `34313411254`), fixed at `8acc14b`; resulting `build-and-test` run `34313919605` passed. Current extraction failure evidence remains the pre-existing live `AuthRequired(Sign in to confirm you're not a bot)` / issue #14 path, not a newly introduced regression.

**State / evidence:** PR #52 is **merged** on `main` at `d581bb9f6a0cbd07449f3c5f8b516ed6a902480c`. The assigned branch had drifted to old history plus a Home-feed probe commit; it has now been reconciled onto current `main` history. The seven documentation commits reported by the previous handoff were **local-only and are not recoverable from GitHub**, so they are not falsely represented as pushed. Current session documentation is being rebuilt on the assigned branch. No extraction fix is merged, released, or hardware-verified. CI/hardware status for the root extraction problem remains **not verified**. The rot-drill red state is pre-existing issue #14 and is not treated as a generic compile gate.

**GitHub evidence:** PR #52 merge commit = `d581bb9`. The previous branch head `6b40b7d` was one commit behind `main` and contained the Home-feed diagnostic probe; it was discarded during reconciliation because the merged Home feature already exists on `main`. Current branch work remains on **`arena/01a08676-dhun` only**. Working PR: **#53**. No release or hardware verification is claimed for the extraction fix.

**Research conclusion so far:** current yt-dlp documentation says PO tokens are attestation credentials generated by BotGuard/DroidGuard/iOSGuard and that affected clients can fail with 403/blocking without them; enforcement varies by client and is actively changing. Source inspection of vivi-music shows its Android stream path uses **InnerTubeX**, supplies a `TokenProvider` backed by a `PoTokenGenerator`, and requests both player-request and streaming-data PO-token values. Its generator uses an Android `WebView` to run BotGuard, obtains an integrity token, creates a PO-token minter, caches the generator/session, recreates it after renderer failures, and caps generation at **8 seconds** so playback can fall through instead of hanging indefinitely. This is strong evidence for the research direction, **not proof that DHUN can copy the implementation unchanged**. See `.ai/EXTRACTION_AUTH_RESEARCH.md` for source URLs and the detailed comparison.

**Exact next technical step:** finish the source-level comparison of vivi-music/InnerTubeX against DHUN's `OwnClientStreamResolver`/`InnerTubeClient`, identify the smallest ADR-compliant integration seam, and draft the required ADR for any extraction identity/token-chain change. In parallel, evaluate two non-identity-chain reliability fixes: persist `DhunStreamCache` across restarts and add an Android outer resolve budget. **Do not implement an extraction identity-chain change until the ADR is approved.**

**Blockers:** the Windows yt-dlp anonymous-vs-authenticated proof requested in the prior handoff has not been supplied yet; no actual Android `adb logcat` playback trace exists; and the sandbox cannot compile locally because it has no JDK/Gradle environment. GitHub access is available through the Arena GitHub integration, but local `git clone` from the sandbox is unavailable due DNS/egress, so repository mutations are being performed through GitHub's repository interface rather than pretending local compilation/push occurred.

---

## Historical session records

The detailed historical snapshots that preceded this reconciliation are retained in the repository's commit history. Important merged milestones: PR #45 two-pane shell → `d0534cd`; PR #46 widgets → `b00a9e1`; PR #47 jump lists/tray → `c13b6c6`; PR #48 equalizer → `789f288`; PR #49 themes was a separate session branch; PR #50 playback diagnostics → `ae212a3` in the older handoff; PR #52 Home Recommended songs → `d581bb9`. These historical records are not current active work.

## Verification rules still in force

- CI is the compile/test gate; never claim local compilation in this sandbox.
- CI green does not close physical audio playback, live Home pagination, visual acceptance, or soak-test gates.
- Do not modify frozen playback engine contracts or the extraction identity chain without the required ADR/evidence process.
- Never conflate **vivi-music** with **ViMusic**.
- Do not merge, close PRs, delete branches, or finalize the session until the user explicitly instructs that action.
