name: rot-drill
# Daily extraction health check vs live YouTube. The tools/playback-probe
# suite exists and compiles in CI (step "Probe compiles"); the scheduled
# LIVE run is still pending wiring (needs a real networked run + alerting).
# Until then this is a placeholder that must FAIL LOUD when someone
# removes this comment and ships no probe.
on:
  schedule:
    - cron: '17 4 * * *'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — replaced by playback-probe run in Phase 01
        run: echo "rot-drill activates in Phase 01 (see MASTER_PROMPT.md)"
